import styled from 'styled-components'

export const Conteudo = styled.div`
  display: flex;
  text-align: center;
  align-items: center;
  margin-bottom: 5vw;
`

export const Logo = styled.img`
  flex-shrink: 0;
  width: clamp(10vw, 100px, 25vw);
  height: auto;
`
export const Titulo = styled.h1`
  flex-grow: 1;
  font-size: 1.3em;
`
